---
title: Instance Configuration
---

The `config.yaml` file is used for basic instance configuration.

This file uses [YAML](https://yaml.org/) markup-language syntax.

## Minimal example

```yaml
// config.yaml

domain: my-evermeet-instance.com
```